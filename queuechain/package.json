{
	"name": "queuechain",
	"version": "0.0.1",
	"description": "",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "npm run sass",
		"sass": "sass ./src/scss/styles.scss ./www/css/styles.css --style compressed --embed-sources",
		"watch": "nodemon -e sass,scss -x \"npm run sass\""
	},
	"author": "AJ Savino",
	"license": "UNLICENSED",
	"dependencies": {
		"nodemon": "^2.0.15",
		"sass": "^1.49.0"
	}
}
